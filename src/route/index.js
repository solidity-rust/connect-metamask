// core components for Admin layout
import Home from '../components/Home';

const routes = [
    {
        path: "/",
        name: "Home",
        component: Home,
        layout: ""
    }
]

export default routes;